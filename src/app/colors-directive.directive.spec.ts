import { ColorsDirectiveDirective } from './colors-directive.directive';

describe('ColorsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
